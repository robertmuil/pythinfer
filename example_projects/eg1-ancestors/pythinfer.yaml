# Example Project 1
# This example project demonstrates the use of SKOS vocabulary.
# Everything is local to make sure it works without internet access,
# and can be used for internal testing.
external_vocabs:
  - ../../vocabs-external/skos.ttl
internal_vocabs:
  - ancestors-model.ttl
data:
  - ancestors-data.ttl